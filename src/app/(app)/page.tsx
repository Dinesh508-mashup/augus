'use client';

import ChatArea from './_components/ChatArea'; // Import ChatArea

// This page now only renders the ChatArea, as Sidebar is in the layout
export default function AppPage() {
  return (
     <ChatArea />
  );
} 